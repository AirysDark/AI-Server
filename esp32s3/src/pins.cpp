#include "pins.h"

namespace Pins {
// Pin assignments are intentionally kept in pins.h so hardware wiring can be
// changed without touching application code. This translation unit exists so
// the project has a dedicated pins.cpp as requested.
}
