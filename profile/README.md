# AI Profile

The AI web UI loads an optional avatar from:

```
profile/photo.jpg
```

The image is not included by default.

Recommended size:

```
256x297 px
```

Any portrait image will work because the UI uses `object-fit: cover` for the circular avatar.